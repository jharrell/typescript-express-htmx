tr
  td #{book.title}
  td #{book.author}
  td 
    button(class="btn btn-primary" hx-get=`/books/${book.id}/edit`) Edit Book
  td 
    button(hx-delete=`/books/${book.id}` class="btn btn-primary") Delete
